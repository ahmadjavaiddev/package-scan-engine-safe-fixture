# Package Scan Engine Safe Fixture

This deliberately small package is the public, lockfile-backed fixture used by
the package scan engine's real-provider end-to-end test.

The `v2/` tree contains inert source-analysis fixtures for the engine's Shadow
Campaign V2. They are test inputs only and must never be installed or executed.
