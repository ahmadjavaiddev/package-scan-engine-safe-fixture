# Shadow Campaign V2 fixtures

These packages are pinned public inputs for provider-backed security scans.
The `safe/` fixtures cover lockfile, cache, and workspace behavior. The
`malicious/` fixtures contain synthetic source patterns with exact ground truth.

Do not install or execute anything below `malicious/`. The scan engine acquires
and analyzes these files without running package code.
